{
  "name": "geckofx",
  "version": "0.0.4",
  "type": "module",
  "bin": {
    "geckofx": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf src/components && tsup src/index.ts --format esm --clean && cp -r src/skills dist"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@types/node": "^26.1.2",
    "cac": "^6"
  },
  "devDependencies": {
    "tsup": "^8",
    "typescript": "^5"
  }
}
